# General configuration
server:
  port: 8080
  servlet:
    context-path: /pmSystem

logging:
  config: classpath:log4j.properties

spring:
   main:    allow-bean-definition-overriding: true
   datasource:
     url: jdbc:mysql://127.0.0.1:3306/pmSystem
     username: root
     password: root
     driverClassName: com.mysql.cj.jdbc.Driver
